<html>
    <head>
        <title><%= title %></title>
    </head>
    <body>
        <h1><%= title %></h1>
        <% messages.forEach((message) => { %>
            <div style="border: 2px solid grey">
                <h2 style="font-size: large"><%= message.username %></h2>
                <p><%= message.content %></p>
                <p style="color: grey"><%= message.creation_date %></p>
            </div>
        <% }); %>
        <a href="/new">Compose Message</a>
    </body>
</html>